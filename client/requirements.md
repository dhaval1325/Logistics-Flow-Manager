## Packages
recharts | Dashboard analytics charts
framer-motion | Page transitions and detailed animations
date-fns | Date formatting for logistics
lucide-react | Icons (already in base, but emphasizing usage)
clsx | Class utility
tailwind-merge | Class utility

## Notes
Integration with OpenAI for POD analysis is mocked via API endpoints if backend integration isn't fully ready, but UI should reflect real-time status.
Data flow: Docket -> Loading Sheet -> Manifest -> THC.
POD flow is separate: Upload -> Analyze -> Approve/Reject.
